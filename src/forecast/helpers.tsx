import { PermitRecord } from "./types";

/**
 * Takes in a project kickoff date and a list of permits and returns the estimated submit and approval dates for each permit
 * Return type is up to you!
 */
export function getPermitForecast(permits: PermitRecord[], kickoff: Date) {
  throw new Error("Not implemented");
}
